 @model IEnumerable<ClassViewModel>

 @{
    ViewData["Title"] = "My Classes";
}


<link rel="stylesheet" href="~/css/index.css" asp-append-version="true" />


<div class="container">
    <div class="row">
        <div class="col-12">
            <table class="table table-image">
                <thead>
                    <tr>
                        <th scope="col">Image</th>
                        <th scope="col">Class Name</th>
                        <th scope="col">Trainer</th>
                        <th scope="col">Start Date</th>
                        <th scope="col">End Date</th>
                        <th scope="col">Unsubscribe </th>
                    </tr>
                </thead>
                <tbody>

                    @foreach (var currentCLass in Model)
                    {
                        <tr>
                            <td class="w-25">
                                <img src="@currentCLass.ImageUrl" class="img-fluid img-thumbnail" alt="Sheep">
                            </td>
                            <td>@currentCLass.ClassName</td>
                            @if (@currentCLass.isDeletedTrainer)
                            {
                                <td>Not have in the moment trainer</td>
                            }
                            else
                            {
                                <td>@currentCLass.Trainer</td>
                            }
                            <td>@currentCLass.StartDateTime.ToString("dd-MM-yyyy")</td>
                            <td>@currentCLass.EndDateTime.ToString("dd-MM-yyyy")</td>
                            <td><a asp-controller="Class" asp-action="RemoveFromClassUser" asp-route-id="@currentCLass.Id" class="btn btn-danger">Unsubscribe</a></td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>